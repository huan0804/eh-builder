# Anon/publishable key của Supabase — public-safe theo mô hình RLS trong
# supabase/schema.sql (mọi ghi/đọc nhạy cảm đi qua RPC security definer).
# KHÔNG BAO GIỜ đặt service_role key ở đây hay ở bất kỳ đâu phía client.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
